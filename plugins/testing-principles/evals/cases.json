[
  {
    "id": "audit-multi-class-pricing",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Review this pricing test and recommend any changes.",
    "context": "PriceQuote coordinates CatalogPrice, CustomerDiscount, and CurrencyRounding. The client-visible rule is that base price 100 with a 10 percent eligible discount produces 90. All collaborators run in process and are fresh per test. The test calls PriceQuote.quote and asserts 90. Runtime is not supplied.",
    "expected_activation": ["testing-principles:audit-tests"],
    "required_findings": ["Several classes can implement one cohesive unit of behavior", "The returned quote is observable output", "Fresh in-process collaborators do not require mocking", "Execution speed is unverified"],
    "forbidden_findings": ["One class is required per unit test", "Every collaborator must be mocked", "The test was measured fast"]
  },
  {
    "id": "design-multi-class-pricing",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Assess whether this pricing design needs new test seams. Propose the smallest design and verification approach; do not write tests.",
    "context": "PriceQuote coordinates CatalogPrice, CustomerDiscount, and CurrencyRounding. The client-visible rule is that base price 100 with a 10 percent eligible discount produces 90. All collaborators run in process and are fresh per test. The test calls PriceQuote.quote and asserts 90. Runtime is not supplied.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Exercise the cohesive behavior through PriceQuote", "Propose checking the observable result 90", "Use fresh collaborators without mock-per-class isolation", "Do not report a pass without execution", "Explain why the existing production boundary is sufficient without adding a new seam"],
    "forbidden_findings": ["Write one test per implementation class", "Verify the exact internal call sequence", "Claim the authored test passed without running it", "Make test authoring the deliverable instead of the production design and verification approach"]
  },
  {
    "id": "audit-observable-account-state",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Audit these withdrawal assertions for value and refactoring resistance.",
    "context": "Account.withdraw changes available_balance, which customers and monthly statements use, and rebuilds _ledger_cache, which is private to Account. The test withdraws 25 from 100, then asserts available_balance is 75 and _ledger_cache contains a particular entry. Runtime is not supplied.",
    "expected_activation": ["testing-principles:audit-tests"],
    "required_findings": ["Both assertions are state-based", "Available balance is observable domain state", "The cache assertion couples to an implementation detail", "Retain the balance assertion and remove or replace the cache assertion"],
    "forbidden_findings": ["All state-based tests are brittle", "A public getter is always an implementation detail", "The cache is observable because a test can access it"]
  },
  {
    "id": "design-observable-account-state",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design for testing the withdrawal behavior. Decide whether the private cache needs to be exposed; describe verification without writing tests.",
    "context": "Account.withdraw changes available_balance, which customers and monthly statements use, and rebuilds _ledger_cache, which is private to Account. The test withdraws 25 from 100, then asserts available_balance is 75 and _ledger_cache contains a particular entry. Runtime is not supplied.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Propose checking available_balance is 75", "Use a state-based assertion at the Account boundary", "Do not assert the private cache", "Report proposed checks as not run", "Explain why the existing production boundary is sufficient without adding a new seam"],
    "forbidden_findings": ["Assert the cache to increase coverage", "Replace the balance assertion with an internal interaction", "Claim demonstrated refactoring survival", "Make test authoring the deliverable instead of the production design and verification approach"]
  },
  {
    "id": "audit-stub-interaction",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Review this test for brittleness.",
    "context": "Checkout asks Catalog for a price, then returns a total. Catalog is an internal query interface. The test supplies price 10, checks total 10, and asserts Catalog.lookup was called exactly once. Lookup call count is not part of the public contract. Runtime is not supplied.",
    "expected_activation": ["testing-principles:audit-tests"],
    "required_findings": ["The supplied lookup result acts as a stub", "Exact lookup count couples the test to an internal query", "Retain the observable total assertion", "Execution speed is unverified"],
    "forbidden_findings": ["All mocks must be deleted", "One class is required per unit test", "The test was measured fast"]
  },
  {
    "id": "design-stub-interaction",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Assess the checkout dependency boundary for testability. Propose any needed production design change and its verification approach.",
    "context": "Checkout asks Catalog for a price, then returns a total. Catalog is an internal query interface. The test supplies price 10, checks total 10, and asserts Catalog.lookup was called exactly once. Lookup call count is not part of the public contract. Runtime is not supplied.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Keep deterministic input through a Catalog stub", "Propose checking the observable total 10", "Remove exact lookup-count verification", "Separate proposed checks from execution evidence", "Explain why the existing production boundary is sufficient without adding a new seam"],
    "forbidden_findings": ["Remove the substitute because a mocking library created it", "Retain call count as proof of total", "Claim the revised test passed without running it", "Make test authoring the deliverable instead of the production design and verification approach"]
  },
  {
    "id": "audit-outgoing-notification-contract",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Review this notification test's double and call-count assertion.",
    "context": "Registration promises a newly registered user exactly one welcome message through an independently operated mail provider. The provider and recipient observe every send. The test registers a user with a sender spy and asserts one welcome send to the new address. Runtime is not supplied.",
    "expected_activation": ["testing-principles:audit-tests"],
    "required_findings": ["The send crosses an unmanaged externally observed boundary", "The spy serves a mock role", "Exactly one send is an observable requirement", "Retain address payload and count assertions"],
    "forbidden_findings": ["All interaction assertions are implementation details", "Call counts are always brittle", "Use the real production mail provider"]
  },
  {
    "id": "design-outgoing-notification-contract",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design the production boundary for testable welcome notifications and explain how to verify the external contract without writing tests.",
    "context": "Registration promises a newly registered user exactly one welcome message through an independently operated mail provider. The provider and recipient observe every send. The test registers a user with a sender spy and asserts one welcome send to the new address. Runtime is not supplied.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Use a spy or mock at the external mail boundary", "Propose checking exactly one welcome send to the address", "Treat the outgoing interaction as observable behavior", "Avoid unrelated internal-step assertions", "Describe the production dependency boundary and how real adapters are supplied"],
    "forbidden_findings": ["Only returned values may be asserted", "Do not verify the contractual count", "Mock every internal collaborator", "Make test authoring the deliverable instead of the production design and verification approach"]
  },
  {
    "id": "audit-managed-database-integration",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Audit this transfer integration test's database treatment.",
    "context": "The Accounts application exclusively owns its database schema; no other application reads it directly, and external access goes through Accounts. The transfer test calls the application API, uses a real temporary database with fresh isolated data, and asserts resulting source and destination balances. Runtime is not supplied.",
    "expected_activation": ["testing-principles:audit-tests"],
    "required_findings": ["The database is out of process and managed", "The API and real-database path is integration scope", "A real managed database is appropriate when practical", "Data isolation is supported but speed is unverified"],
    "forbidden_findings": ["All databases must be mocked", "Out of process means unmanaged", "Temporary means measured fast"]
  },
  {
    "id": "design-managed-database-integration",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design the transfer flow for testability at the application boundary, including production composition and a verification approach.",
    "context": "The Accounts application exclusively owns its database schema; no other application reads it directly, and external access goes through Accounts. The transfer test calls the application API, uses a real temporary database with fresh isolated data, and asserts resulting source and destination balances. Runtime is not supplied.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Choose an integration test through the Accounts API", "Use the real managed database when practical", "Isolate each test's data", "Propose checking balances instead of SQL or repository calls", "Describe the production dependency boundary and how real adapters are supplied"],
    "forbidden_findings": ["Mock the database solely because it is out of process", "Verify exact persistence calls", "Describe runtime as measured fast", "Make test authoring the deliverable instead of the production design and verification approach"]
  },
  {
    "id": "audit-unknown-database-ownership",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Review this database-write test's interaction assertion.",
    "context": "OrderCompletion writes a completed order row to a database. The test substitutes the repository and verifies the exact insert payload. Materials do not say who owns the schema, whether another service or report reads it directly, or whether access is hidden behind OrderCompletion. Runtime and fixture lifecycle are not supplied.",
    "expected_activation": ["testing-principles:audit-tests"],
    "required_findings": ["Ownership and external observers are unknown", "Exclusive hidden ownership would make it managed and favor a real integration path", "Direct external consumers could make the write an observable contract", "Request ownership consumers and compatibility promises"],
    "forbidden_findings": ["A database is always managed", "A database is always unmanaged", "The repository verification is definitely correct or brittle"]
  },
  {
    "id": "design-unknown-database-ownership",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design a testable boundary for completed-order persistence. Keep the production design conditional on the missing ownership facts.",
    "context": "OrderCompletion writes a completed order row to a database. The test substitutes the repository and verifies the exact insert payload. Materials do not say who owns the schema, whether another service or report reads it directly, or whether access is hidden behind OrderCompletion. Runtime and fixture lifecycle are not supplied.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Keep the recommendation conditional", "Use a real database path if ownership is exclusive and hidden", "Verify the write contract if independent consumers rely on it", "Identify ownership consumers promises and isolation as missing facts"],
    "forbidden_findings": ["Choose a mock unconditionally", "Choose a real database unconditionally", "Invent an external consumer", "Make test authoring the deliverable instead of the production design and verification approach"]
  },
  {
    "id": "classify-explicit-output-test",
    "mode": "classify",
    "invocation": "explicit",
    "prompt": "Explicitly invoke classify-tests and classify this test across every required dimension.",
    "context": "InvoiceTax calculates amount due from a subtotal and jurisdiction table. The test constructs fresh in-process objects, calls amount_due for 100, and asserts 108, which is shown to the customer. No command, duration, or hidden-state evidence is supplied.",
    "expected_activation": ["testing-principles:classify-tests"],
    "required_findings": ["The assertion is output-based and observable", "Known dependencies are private and in process", "Unit scope is conditional because speed and hidden shared state are unverified", "Assess quality pillars separately"],
    "forbidden_findings": ["A returned value proves unit scope", "One assertion guarantees maintainability", "The test ran quickly"]
  },
  {
    "id": "classify-mixed-unmeasured-runtime",
    "mode": "classify",
    "invocation": "implicit",
    "prompt": "What kind of test is this, and what can we conclude about its quality?",
    "context": "A checkout test posts to the API, checks a returned receipt, reads a test-specific row from the application's exclusively owned database, and verifies one request to an external payment provider. Database data is unique and cleaned afterward. No duration or observed refactor is supplied.",
    "expected_activation": ["testing-principles:classify-tests"],
    "required_findings": ["Assertions are mixed output state and communication", "The API and real database path supports integration scope", "The database is managed while payment is unmanaged", "Speed and refactor survival are unverified"],
    "forbidden_findings": ["Mixed assertions cause integration scope", "A payment mock causes unit scope", "Cleanup proves fast execution"]
  },
  {
    "id": "audit-shared-state-pollution",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Find the reliability problem and recommend a focused correction.",
    "context": "Two balance tests reuse one mutable module-level Account. One withdraws 20 and expects 80; another deposits 10 and expects 110. There is no reset. Each passes alone from 100, but the second fails after the first. Assertions inspect client-visible balance. Durations are absent.",
    "expected_activation": ["testing-principles:audit-tests"],
    "required_findings": ["The Account is shared mutable test state", "Order dependence demonstrates failed isolation", "Create a fresh Account or reliable reset per test", "Retain the meaningful balance assertions", "Speed is unverified"],
    "forbidden_findings": ["Delete the balance assertions", "Parallel execution is the only fix", "Sharing a class definition is the problem"]
  },
  {
    "id": "design-unfamiliar-language-regression",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design the parser boundary for testability and identify repository facts needed before proposing production changes. Do not write tests or fix the parsing defect.",
    "context": "This is a Gleam repository. The parser accepts the unterminated input `[1, 2` instead of returning a parse error. No parser module or function signature, error representation, nearby test source, helper API, or test command is supplied. The Gleam runtime is unavailable and no installation was requested.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Identify the parser entrypoint and observable error as the candidate boundary", "Distinguish the parsing defect from evidence of a testability obstacle", "Request the parser signature, error representation, source and composition conventions before recommending changes", "Describe malformed input verification without inventing APIs or execution results"],
    "forbidden_findings": ["Invent Gleam test syntax module names helper calls or error constructors", "Install Gleam globally", "Claim the regression test failed or passed", "Make test authoring the deliverable instead of the production design and verification approach"]
  },
  {
    "id": "unrelated-documentation-edit",
    "mode": "unrelated",
    "invocation": "implicit",
    "prompt": "Rewrite this release-note paragraph so it is concise and user-facing.",
    "context": "Draft: We made internal adjustments to navigation wording so users can locate billing history more easily. No code, test, classification, or coverage work is requested.",
    "expected_activation": [],
    "required_findings": ["Return a concise user-facing release note"],
    "forbidden_findings": ["Add unrequested test-classification analysis", "Recommend test doubles", "Add an unrequested test audit"]
  },
  {
    "id": "unrelated-explicit-fixture-plugin",
    "mode": "unrelated",
    "invocation": "explicit",
    "prompt": "Explicitly invoke fixture-docs:write-release-notes and produce one release-note bullet.",
    "context": "Both fixture-docs and testing-principles are installed. The change renames Billing History to Payment History. The disposable fixture skill applies only to release-note editing. No testing task is requested.",
    "expected_activation": ["fixture-docs:write-release-notes"],
    "required_findings": ["Produce one release-note bullet that accurately describes the label rename"],
    "forbidden_findings": ["Add unrequested testing advice", "Claim source code or tests were changed"]
  },
  {
    "id": "design-explicit-python-cart-fixture",
    "mode": "design",
    "invocation": "explicit",
    "prompt": "Explicitly invoke design-for-testing and follow the fixture README to assess the Python cart design. Do not change source or write tests.",
    "context": "The workspace contains the source and README copied from the Python cart fixture. The implementation contains a deliberate behavior defect.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Recognize the pure subtotal input and returned total as a sufficient existing boundary", "Identify the free-shipping threshold defect separately from testability", "Propose boundary checks at 49, 50 and 51 with expected totals 54, 50 and 51", "Keep source unchanged and author no tests", "Separate proposed verification from observed execution"],
    "forbidden_findings": ["Add unnecessary dependency abstractions", "Fix the deliberate defect or write test files", "Claim proposed checks were executed"]
  },
  {
    "id": "design-explicit-js-notifier-fixture",
    "mode": "design",
    "invocation": "explicit",
    "prompt": "Explicitly invoke design-for-testing and follow the fixture README to assess the JavaScript notifier design. Do not change source or write tests.",
    "context": "The workspace contains the source and README copied from the JavaScript notifier fixture. The implementation contains a deliberate behavior defect.",
    "expected_activation": ["testing-principles:design-for-testing"],
    "required_findings": ["Recognize the injected sender as a sufficient existing seam", "Identify the duplicate welcome send as a behavior defect rather than missing testability", "Describe checking one Welcome message to the requested address", "Explain that sender-only checks leave production composition and real delivery unverified", "Keep source unchanged and author no tests", "Separate proposed verification from observed execution"],
    "forbidden_findings": ["Add unnecessary dependency abstractions", "Fix the deliberate defect or write test files", "Claim proposed checks were executed"]
  },
  {
    "id": "design-production-seams-before-tests",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design production code for test seams before any tests exist. Propose the production changes and verification approach; do not implement yet.",
    "context": "finalize(id) constructs SqlInvoices and MailClient internally, loads an invoice, computes its preferred-customer discount inline, saves the total, and sends one receipt. The database is exclusively application-owned; mail is independently operated. Preserve finalize(id). A failed save must propagate and prevent sending. A teammate proposes interfaces for every helper and tests only of injected objects.",
    "required_findings": [
      "Identify internal I/O construction and mixed decisions/effects as testability obstacles",
      "Propose cohesive deterministic pricing logic and minimal boundary injection without one interface per helper",
      "Preserve finalize(id) and route production through the same tested logic",
      "Preserve save-failure propagation and prevention of receipt sending",
      "Propose a production-entrypoint integration check using real managed persistence and observed external receipt behavior",
      "Keep the response design-only"
    ],
    "forbidden_findings": [
      "Require interfaces for every helper",
      "Claim injected-object tests alone verify production composition",
      "Replace all persistence evidence with repository-call assertions",
      "Claim proposed tests were executed"
    ],
    "expected_activation": [
      "testing-principles:design-for-testing"
    ]
  },
  {
    "id": "design-no-new-seam-needed",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Assess whether checkout needs more production test seams. Describe the design and verification approach without writing tests.",
    "context": "Checkout already takes basket data and uses real deterministic DiscountPolicy and MoneyRules objects with no I/O or hidden input. Preferred customers get ten percent off. One proposal introduces an interface for every helper and verifies internal calls. No runtime or source files are supplied.",
    "required_findings": [
      "Keep the existing production design because inputs are already controllable",
      "Exercise real collaborators together and assert customer-visible total",
      "Reject interfaces and mocks added solely to isolate each helper",
      "Report execution as unverified",
      "Explain why the existing production boundary is sufficient without adding a new seam"
    ],
    "forbidden_findings": [
      "Introduce a seam for each class",
      "Verify internal helper calls as the pricing outcome",
      "Claim the tests passed",
      "Make test authoring the deliverable instead of the production design and verification approach"
    ],
    "expected_activation": [
      "testing-principles:design-for-testing"
    ]
  },
  {
    "id": "design-legacy-seam-baseline-gap",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Refactor this hard-to-test receipt flow for testability and describe how to verify behavior preservation.",
    "context": "The flow constructs an external client internally and has no existing safe test path. Live credentials and the provider are unavailable. Production edits are authorized. Its documented behavior is to save first, propagate a save failure without sending, and otherwise emit one receipt. The repository supports passing a sender callable. No executable source is supplied.",
    "required_findings": [
      "Explain that a safe pre-change execution baseline is unavailable",
      "Propose the smallest sender seam using existing callable conventions",
      "Preserve save-before-send and error behavior",
      "Describe verification that the actual production path uses the seam and report unavailable adapter or provider checks",
      "Distinguish proposed or post-change checks from demonstrated before/after regression evidence"
    ],
    "forbidden_findings": [
      "Claim a pre-change failure was observed",
      "Use live production credentials",
      "Add a test-mode branch that bypasses sending",
      "Claim full production integration from a sender fake"
    ],
    "expected_activation": [
      "testing-principles:design-for-testing"
    ]
  },
  {
    "id": "tests-only-production-constraint",
    "mode": "write",
    "invocation": "implicit",
    "prompt": "Review and improve these tests only; do not change production code.",
    "context": "An invoice exposes its final total to customers and caches a private subtotal. All calculation helpers are deterministic and accept ordinary data. The current test uses reflection to inspect the private cache. No test source or runtime is supplied.",
    "required_findings": [
      "Respect the prohibition on production changes",
      "Assert customer-visible final total through the existing boundary",
      "Keep real deterministic helpers rather than adding doubles",
      "Avoid inventing framework APIs or execution results"
    ],
    "forbidden_findings": [
      "Expose a public cache accessor",
      "Add production test seams despite the tests-only scope",
      "Retain private cache inspection as the main behavior assertion"
    ],
    "expected_activation": [
      "testing-principles:audit-tests"
    ]
  },
  {
    "id": "audit-diagnostic-logging",
    "mode": "audit",
    "invocation": "implicit",
    "prompt": "Review this test; should its logging assertion stay?",
    "context": "Checkout returns 90 and logs discount applied. The test checks 90 and exact debug wording. The message is only an internal diagnostic; no consumer contract relies on it. Runtime is not supplied.",
    "expected_activation": [
      "testing-principles:audit-tests"
    ],
    "required_findings": [
      "Retain the returned total assertion",
      "Remove the incidental wording assertion",
      "Original assertions are mixed output and communication",
      "Do not infer measured speed"
    ],
    "forbidden_findings": [
      "Remove the production log merely because the assertion is brittle",
      "All logging must be mocked"
    ]
  },
  {
    "id": "design-support-logging",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design a testable production boundary for the account-change support event and describe how to verify its contract.",
    "context": "An external support tool requires account_id, old_status, new_status, exactly one event per actual transition and none for unchanged status. Delivery is an unmanaged boundary. A teammate proposes deleting all logger tests as implementation details. No executable project is supplied.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "Preserve coverage of the required event",
      "Verify required fields and both emission conditions",
      "Use an appropriate recorder or mock at the outgoing boundary",
      "Distinguish emission verification from real delivery evidence",
      "Describe the production dependency boundary and how real adapters are supplied"
    ],
    "forbidden_findings": [
      "All logs are implementation details",
      "Assert incidental human-readable wording",
      "Claim delivery passed without execution",
      "Make test authoring the deliverable instead of the production design and verification approach"
    ]
  },
  {
    "id": "design-hidden-logger",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Make this service testable without writing diagnostic files.",
    "context": "A service internally constructs a static file logger. Its result is the behavior under test. A proposed isTestEnvironment switch would skip writes. Production must continue using its real logger.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "Reject the test-environment bypass switch",
      "Use the smallest explicit logging seam",
      "Supply a no-op substitute for incidental logging",
      "Preserve production composition and distinguish it from substitute-only evidence"
    ],
    "forbidden_findings": [
      "Introduce a test-only production branch",
      "Mock verification is mandatory for every logger",
      "The no-op proves real file delivery"
    ]
  },
  {
    "id": "classify-unknown-logger",
    "mode": "classify",
    "invocation": "implicit",
    "prompt": "Classify this test and assess the value of its assertion.",
    "context": "The test verifies logger.info through a fresh in-memory spy. No consumer, storage, application-boundary, or runtime information is supplied.",
    "expected_activation": [
      "testing-principles:classify-tests"
    ],
    "required_findings": [
      "Communication-based assertion",
      "Spy verifies an outgoing interaction",
      "Keep observability conditional on consumer and contract",
      "Do not infer scope or measured speed from the spy"
    ],
    "forbidden_findings": [
      "The info level establishes a business contract",
      "All logging assertions should be removed",
      "The spy guarantees unit scope"
    ]
  },
  {
    "id": "ordinary-test-authoring-no-design",
    "mode": "unrelated",
    "invocation": "implicit",
    "prompt": "Write a standard-library unittest for add(a, b). Do not review the design or change production code.",
    "context": "add(a, b) returns a + b and has no dependencies or hidden state. No runtime is supplied.",
    "expected_activation": [],
    "required_findings": [
      "Provide a focused test for the existing function",
      "Report execution as not run"
    ],
    "forbidden_findings": [
      "Invoke design-for-testing solely because tests were requested",
      "Introduce production seams or abstractions",
      "Claim a test passed without execution"
    ]
  },
  {
    "id": "design-ambient-input-timing",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design this expiry flow for deterministic testing without changing its behavior. Propose the design only.",
    "context": "The flow reads the clock before a slow fetch and again after it to decide whether to send. Production must keep both readings. A teammate suggests accepting one timestamp for the whole operation. The repository permits passing a clock callable.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "Use a callable clock seam preserving both acquisition points",
      "Reject replacing both reads with one snapshot",
      "Describe wiring the real clock in production",
      "Propose controlled before/after times and composition verification without writing tests"
    ],
    "forbidden_findings": [
      "Collapse both clock reads into one timestamp",
      "Add a test-mode branch",
      "Claim execution evidence"
    ]
  },
  {
    "id": "design-guided-registration-decisions",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Walk me through how to decide test types and whether to introduce interfaces or doubles for our registration design. Design only.",
    "context": "Registration loads account data from our exclusively owned PostgreSQL database, runs deterministic eligibility rules across three concrete classes, queries an independently owned read-only risk API, persists acceptance, then sends exactly one welcome email through an external provider after commit. No other clients access the database. Risk client and mail sender are already parameters; database operations and decision logic are in the same handler. A teammate wants an interface and mock for every class to make all tests unit tests. We also need confidence in HTTP routing and production composition. Tests are not written yet and no timings are available.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "Explain the behavior and application boundary before choosing coverage",
      "Keep the three real classes together for intended unit decision checks and mark speed unverified",
      "Classify dependency process location ownership and planned test isolation separately",
      "Choose integration coverage with real managed PostgreSQL and assert persisted outcomes",
      "Use risk responses as stub inputs and the recorded welcome send as a mock role",
      "Explain test double as an umbrella term rather than a separate substitute choice",
      "Reuse existing risk and mail seams and separate decisions from I/O without adding an interface per class",
      "Explicitly decide whether a broad end-to-end path adds HTTP routing and composition evidence and state substituted external boundaries",
      "Preserve commit-before-send and describe distinct faults detected by the coverage"
    ],
    "forbidden_findings": [
      "Mock every class to obtain unit scope",
      "Verify internal risk lookup counts as the registration contract",
      "Treat a mocked database as proof of persistence",
      "Claim proposed tests were run"
    ]
  },
  {
    "id": "design-interface-real-variation",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Decide whether this design warrants an interface, test doubles, and unit or integration coverage. Propose only.",
    "context": "PriceQuote supports two actual production discount policies selected by tenant configuration. Both run deterministically in process on explicit values, implement the same customer-visible policy contract, and can be freshly constructed per test. There is no I/O. Repository conventions permit a small policy protocol. No test timings are available.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "A policy interface can express actual production variation",
      "Do not replace deterministic policies with mocks merely because an interface exists",
      "Propose intended unit checks of customer-visible outputs with real policies",
      "Mark speed unverified and describe isolation",
      "No integration or end-to-end test is required merely to include every test type"
    ],
    "forbidden_findings": [
      "Interfaces are only justified for external mocks",
      "Every implementation needs a test double",
      "Require one test of every scope"
    ]
  },
  {
    "id": "design-unmanaged-interface-needed",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design a testable notification boundary and explain the test scope, interface, and double choices. Do not implement.",
    "context": "A handler directly constructs a sealed SDK client for an independently operated SMS provider. The customer contract requires one message to a specified number for a successful action. The statically typed repository cannot substitute this client and has no existing sender abstraction; its composition conventions support a small injected protocol. Provider access is unavailable in tests.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "Introduce a narrow sender contract justified by necessary substitution at the unmanaged boundary",
      "Supply the real SDK adapter through normal production composition",
      "Use a mock or spy for the promised recipient payload and count",
      "Explain that interface and double presence do not determine test scope",
      "Describe adapter verification with safe controlled transport when feasible and name missing provider compatibility evidence"
    ],
    "forbidden_findings": [
      "Never introduce an interface with one production implementation",
      "Mock each internal helper",
      "Claim substitution proves live delivery"
    ]
  },
  {
    "id": "design-unknown-ownership-walkthrough",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Help choose test types and decide whether to add a repository interface or use a real dependency in this design. Design only.",
    "context": "The service writes to a PostgreSQL table. Code and deployment ownership are unavailable. It is unknown whether reporting jobs read the table directly. The only supplied behavior is that a successful request stores an accepted record. No fixture lifecycle or timings are supplied.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "Ask a focused ownership or external-consumer question or explicitly retain conditional branches",
      "If exclusively owned and hidden use real managed persistence for integration coverage when practical",
      "If independently observed identify the relevant writes as an external contract before choosing verification",
      "Leave shared/private and speed unresolved without evidence",
      "Do not prescribe a repository interface before establishing the need"
    ],
    "forbidden_findings": [
      "All databases are managed",
      "Out-of-process dependencies are always unmanaged",
      "Invent fixture isolation",
      "Require the user to answer a fixed questionnaire before any useful guidance"
    ]
  },
  {
    "id": "design-constrained-managed-dependency",
    "mode": "design",
    "invocation": "implicit",
    "prompt": "Design coverage and decide whether a new repository interface is justified under this constraint. Do not write tests.",
    "context": "An application exclusively owns a database and no other consumers access it. This environment cannot run the database. Domain decisions already accept explicit values and produce results. An existing repository parameter can accept a simplified in-memory implementation in tests. No real database evidence or timings are available.",
    "expected_activation": [
      "testing-principles:design-for-testing"
    ],
    "required_findings": [
      "Keep the existing pure decision boundary and real internal collaborators",
      "Reuse the existing repository seam if substitution is useful rather than adding an interface",
      "Describe the simplified implementation as a fake and determine its stub or mock role by use",
      "Do not verify internal persistence calls as a substitute for observable results",
      "Separate substitute-based coverage from the pending real-database integration check and state lost fidelity"
    ],
    "forbidden_findings": [
      "Claim full persistence integration from the fake",
      "Introduce an interface solely because every dependency needs one",
      "Claim measured fast feedback"
    ]
  }
]
